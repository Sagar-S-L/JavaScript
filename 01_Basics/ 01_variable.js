const pi=3.142;//constant value declaration
let num=10;//we can change the value if we declare the variable as let
var id=101;//we can change the value if we declare the variable as var
name="Sagar";// we can declare without any keyword but dont use it
let accountState;
console.table([pi,num,id,name,accountState]);

/* Note:-
prefer not to use var because of issue in block scope and functional scope

*/